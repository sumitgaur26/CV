// Update this once the site has a real production domain (or set
// NEXT_PUBLIC_SITE_URL at deploy time). Used for the canonical URL,
// Open Graph metadata, and the JSON-LD Person schema.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL ?? "https://sumitgaur.dev";
